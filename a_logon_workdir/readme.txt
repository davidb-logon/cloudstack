FIXED (issue #1): packaging/build-deb.sh to not concatanate the ubuntu ditribution (jammy) after
       every run of the script
ADDED:
(issue #3) 
    Rules file for doing build for packaging w/o tests and with sources.
    Changed the main pom.xml file to include a profile generate-sources
    with the maven-source-plugin



